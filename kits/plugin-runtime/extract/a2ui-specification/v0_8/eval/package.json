{
  "name": "a2ui_genkit_eval",
  "version": "1.0.0",
  "main": "lib/index.js",
  "scripts": {
    "test": "node -e 'const fs=require(\"fs\"); const path=require(\"path\"); function find(d){for(const f of fs.readdirSync(d)){const p=path.join(d,f);if(f===\"node_modules\"||f===\"dist\")continue;if(fs.statSync(p).isDirectory()){if(find(p))return true;}else if(f.includes(\".test.\")||f.includes(\".spec.\"))return true;}return false;} if(find(\".\")){require(\"child_process\").execSync(\"vitest run\",{stdio:\"inherit\"});}else{console.log(\"Workspace has no tests.\");}'",
    "build": "wireit",
    "eval": "dotenv -- tsx src/index.ts",
    "evalAll": "yarn eval",
    "evalGemini": "yarn eval -- --model=gemini-2.5-flash --prompt=travelItinerary",
    "evalGpt": "yarn eval -- --model=gpt-5-mini --prompt=travelItinerary",
    "evalClaude": "yarn eval -- --model=claude-4-sonnet --prompt=travelItinerary",
    "start": "genkit start",
    "genkit:dev": "genkit start -- tsx --watch src/dev.ts",
    "format": "prettier --write src/**/*.ts",
    "format:check": "prettier --check .",
    "lint": "echo \"Workspace has no lint configuration.\"",
    "lint:fix": "echo \"Workspace has no lint configuration.\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/yargs": "^17.0.35",
    "dotenv-cli": "^11.0.0",
    "prettier": "^3.8.4",
    "tsx": "^4.22.4",
    "typescript": "5.9.3",
    "wireit": "^0.15.0-pre.2",
    "yargs": "^18.0.0"
  },
  "dependencies": {
    "@genkit-ai/compat-oai": "^1.36.0",
    "@genkit-ai/google-genai": "^1.36.0",
    "@genkit-ai/vertexai": "^1.36.0",
    "genkit": "^1.36.0",
    "genkitx-anthropic": "^0.30.0"
  },
  "wireit": {
    "build": {
      "command": "tsc",
      "files": [
        "src/**/*",
        "tsconfig.json"
      ],
      "output": [
        "lib/"
      ]
    }
  }
}
