# Changelog

All notable public changes are listed here. Release tags are the source of truth for exact code state.

## Unreleased

- Documentation and contribution workflow: record the cross-boundary lessons from the typed-abstraction audit; define stable identity, single-owner lifecycle, monotone per-metric coverage, independent telemetry, exact persistence, and compatibility-seam rules; add a coherence checklist for new abstractions; and require every stacked PR tip to be standalone and fully validated before GitHub's native asynchronous stack merge advances the chain in base-to-top order.
- Typed boundary program: automatically check every shipped Python source with warning-free `ty`; preserve precise case/model/repetition identities and explicit binary contrast coordinates through pairing; require frozen subprocess plans; validate exact artifact/event schema integers and recursively freeze persisted JSON; carry explicit judge invocation lifecycle without fabricating return codes; classify assertions, judge tasks, report attempts, per-metric coverage, empty and non-applicable populations as closed values; and validate every CLI command through a deeply frozen `ValidatedLegacyCLIInvocation`. Existing CLI handlers still cross one named Namespace compatibility adapter. Packaging, static-analysis coverage, and causal identity now have separate inventories; trigger `harness_identity` schema version 2 uses a conservative audited module inventory. Standalone non-trigger modules are excluded, while any edit to the shared `skill_benchmark.py` monolith still invalidates trigger identity. Previously accepted malformed programmatic rows, boolean/float schema versions, duplicate report identities, missing metric survivor-means, and contradictory judge process states now fail or remain unavailable; regenerate affected prepared/run/report artifacts and version-1 trigger reports rather than coercing them.
- Gemini CLI backend: add `run-agent --agent gemini` and `judge --judge-backend gemini` through the unified registry, with isolated `GEMINI_CLI_HOME`, minimal auth seeding, sandbox requests, deny-by-default tool policies, strict frozen JSON/stream-JSON contracts, normalized per-model usage, provider transcript sidecars, a Gemini trace dialect, offline upstream conformance fixtures, and `RUN_GEMINI_SMOKE`. Malformed/missing/contradictory terminal evidence, provider errors, spawn failures, nonzero exits, and timeouts fail closed; missing usage/cost stays missing. Autonomous trigger remains deliberately unadvertised because a safe consent-free headless `activate_skill` run has not been live-proven.
- Unified backend registry: explicit answer routes and executable command entrypoints; native answer, trigger, and judge bindings; provider CLI options; capabilities; workspace builders; lazy trace-dialect implementations; smoke targets; and failure markers now have one immutable owner in `agent_capabilities.BACKENDS`. Implementation dispatch maps remain replacement-only compatibility projections, policy projections are deeply immutable with closed scalar vocabularies, and `skill-benchmark agent-capabilities` exposes capabilities separately from native command bindings so Jetty export/import and the dedicated subagent runner are not misreported as unsupported. Registry construction fails on partial routes, trace-binding drift, invalid lazy references, export/import entrypoints owned by another backend, invalid failure policy, or CLI collisions; materialization rejects answer, trigger, judge, and workspace references that do not satisfy their runtime contracts before publishing compatibility views; direct-script execution preserves canonical implementation identity, and answer runs consume their registered workspace builders. The architecture, backend-interface, control-plane, extension, parity, vocabulary, command, TODO, and Lessons Learned documentation now describe the same owner and two-stage validation boundary.
- Jetty attempts are now crash-resumable. Live `run-jetty` requires `--out`, canonicalizes journal aliases, rejects unsafe lock identities, takes an OS-released exclusive lock, and transactionally journals the attested payload identity, uploaded bundle, allowlisted acknowledgement/poll receipts, downloaded artifacts, and committed result; a restart reconstructs all durable output before replacement and resumes the last safe boundary instead of issuing another paid submission. A connection loss, unproven HTTP rejection, or unusable response while the submit acknowledgement is pending becomes `submission_unknown` and stays non-retriable unless the operator explicitly passes `--resubmit-unknown`; only provider-documented 429 rejection returns to the automatically submit-ready state. A local polling deadline leaves the acknowledged trajectory resumable, emits a nonterminal record, exits nonzero, and cannot be imported. Jetty does not currently expose a server-side idempotency key, so the ambiguous-response window is reported rather than presented as exactly-once execution.
- Trigger aggregation now retains typed observations through the reporting boundary. Complete, incomplete, and empty cohorts are distinct variants, and only complete cohorts possess pass or trigger rates; incomplete CLI runs are explicit and exit nonzero.
- Typed judge invocations: native Claude/Codex/Vibe judges and the universal `--judge-cmd` escape hatch now return one frozen `JudgeInvocation` contract before verdict parsing. The boundary validates and recursively freezes stdout/stderr, return code, usage, cost provenance, and model identity; a registered backend that still returns a dictionary is rejected immediately. Persisted judge-result rows and CLI behavior are unchanged.
- Developer tooling: expand the pinned `ty` gate from the clean contract and registry seams to the central `skill_benchmark.py` CLI; replace its temporary identity-aware 38-diagnostic regression budget with a permanent zero-diagnostic project check, and keep CI and contributor validation on that single owner without exclusions or blanket ignores.
- Human-text assertions are now correct by construction: immutable `ComparisonText` values preserve raw artifacts while a versioned `rendered-v1` comparison view removes only `U+200B ZERO WIDTH SPACE`, `U+2060 WORD JOINER`, and `U+FEFF ZERO WIDTH NO-BREAK SPACE`, then applies NFC. Direction-changing bidi controls and soft hyphens remain exact because erasing them can change visible output. `contains`/`regex` positives no longer false-fail on zero-width formatting, and `excludes_any`/`not_regex` cannot be evaded with the same characters. Results disclose normalization and deterministic verdict changes (`null` for embedding similarity, where a raw verdict would require another external call); `comparison: "exact"` opts out through the same closed manifest schema as the default profile. The same view closes invisible-character gaps in leakage and contamination checks, and their minimum-length/non-vacuity gates now consume that view too. Strict typed assertion parsing rejects missing regexes, rendered-empty operands and canaries, conflicting operand aliases, scalar/empty value lists, non-boolean `ci`, invalid comparison modes, non-finite similarity thresholds, invalid golden normalization, and invalid process regexes before grading, while retaining the legacy list-valued `value` alias for multi-value literals. Regex source must be stable under its selected profile; every rendered-v1 regex verdict uses exact-pinned `regex==2026.7.19` in `VERSION0` compatibility mode under a monotonic 0.25-second operation budget, so inserting a removable control cannot switch engines or Unicode character-class semantics. When normalization changes a candidate, its normalized and raw-diagnostic searches share that budget. Timeout, resource exhaustion, or non-CPython execution yields partial/unavailable evidence rather than a passing absence check. The bounded path works in worker threads and owns no process-global timer or handler; `comparison: "exact"` retains stdlib `re` behavior. Similarity verdicts derive from the published four-decimal score; embedding vectors must be finite non-boolean numbers and negative cosine maps to the 0.0 score floor; missing similarity artifacts fail closed. Graded-script score lines likewise reject boolean/non-finite scores and maxima. Process/tool/JSON/golden boundaries remain exact. This deliberately makes negative human-text checks stricter and can correct historical scores when old outputs are regraded; four-decimal thresholding can also change a historical verdict at the rounding boundary.
- Claude answer traces: `run-claude`/`run-agent --agent claude` now drive `claude -p --output-format stream-json --verbose` and keep the stream as the run's raw trace, so Claude answer runs carry real tool-use trajectory evidence — `parse_claude_cli_json` resolves both output formats through one parser (the terminal `type:"result"` event is the envelope; a stream that dies before it stays a protocol failure), and the Claude trace dialect pairs a `tool_use` with its `tool_result` so an orphaned call counts zero and cumulative usage is counted once. Completed events point `raw_ref` at the physical invocation line and `raw_result_ref` at the physical result line; blank/malformed JSONL lines no longer shift references. Claude Read/Write/Edit keep file-operation taxonomy, generic tool arguments remain available untruncated to per-step judges, and `tool_result.is_error` contributes error evidence. Every other trace dialect keeps the identity flatten and normalizes byte-identically; `import-trace --source claude` accepts external streams.
- Trace dialect registry: per-provider trace semantics (flatten, terminal usage/failure, stream usage/cost blocks) now live in one registry (`TRACE_DIALECTS`) consumed by `normalize_trace_records` and `stream_usage_and_cost`, replacing scattered Pi branches and the Claude flatten special case; generic-semantics sources must register explicitly, and unknown or misspelled sources are rejected.
- Significance inside the causal door: `causal_confirmation` gains the replication gate as a `significant` parameter, so an observed-but-insignificant regression is `indeterminate` inside the guard and future consumers cannot forget the downgrade; both the answer-path ablation report and `trigger-compare` route through it, while `trigger-compare` requires a significant aggregate drop.
- Doc-reference fixer: `scripts/fix_doc_refs.py` now owns the doc code-reference grammar and rewrites stale `name:line` citations in place; `tests/test_doc_refs.py` imports the same logic, so checker and fixer cannot disagree.
- Per-step trajectory judging: a case-level judge assertion may declare `per_step` (true, or `{min_met_fraction: f}`) to grade EACH completed trajectory step on the existing dynamic-criteria verdict shape — one criterion per step, `minimum_criteria` derived with exact decimal ceiling from the run's actual step count, step payloads carrying separate untruncated invocation/result records. Missing step evidence fails closed at BOTH ends: grade emits no judge task (no model spend) and a re-run judge task refuses to invoke. Verdicts bind to a SHA-256 of the exact trajectory payload and are re-queued if the trace, step names, or minimum changes; malformed repeats retain the dynamic verdict shape instead of crashing aggregation. Empty-object configuration and turn-level `per_step` are rejected during validation.
- Trigger causal pairing: new `skill-benchmark trigger-compare` pairs a baseline skill-trigger-matrix report with an `--ablation` report of the same canonical revision — rows re-validated through `TriggerObservation.from_row`, exact design/repetition cardinality enforced, manifest treatment identity and experimental protocol self-digested and matched, observed isolation drift blocked, complete agent/model cells collapsed to one inference unit per authored query/polarity before the sign-flip test, and verdict routed through the `causal_confirmation` evidence guard. Confirmation requires matching top-level/recorded ablation IDs, mounted-tree/provenance integrity, a negative aggregate mean, and conservative significance; invalid-skill treatments remain indeterminate, incomplete observations never enter pass-rate denominators, adding models cannot manufacture sample size, and a significant improvement cannot be mislabeled a causal regression. Legacy trigger reports without the new identity/protocol fields must be regenerated.
- Trajectory diff: `benchmark.json` gains a `trajectory_diff` block — per case, over validated experimental pairs, commands exclusive to one arm across all paired repetitions, completed-event count deltas, and per-arm skill-load rates; an arm without non-empty readable trace evidence blocks its pair with a named reason instead of reading as an empty diff. `tool_count_le` likewise counts completed lifecycle events once rather than counting start and completion records separately.
- Jetty adapter validated against production: the first token-backed run of `export-jetty -> run-jetty -> import-jetty-results -> benchmark` (2026-07-17) found and fixed four pieces of mock-reality drift — the assumed `POST /api/v1/files/upload` endpoint does not exist (uploads now zip the whole mount plan through `POST /api/v1/sandbox/upload`, the only shape that preserves directory trees, auto-extracted under `/app/assets/`); model-visible paths are now deterministic `/app/assets/...` values baked at export time instead of run-time storage keys; trajectory ids are read from `jetty_metadata` (including the 202 `workflow_id` form `<collection>-<task>--<id>`, whose bare suffix is what the trajectory routes key on); and every request sends a real `User-Agent` because Cloudflare bans urllib's default signature outright. `run-jetty` now submits with a short `jetty.timeout_hint`, polls the DB record with 404 grace, fetches the storage detail on success, downloads each `results_files[]` artifact (text inlined, binary as base64), and normalizes the live `usage` block (tokens, `cost_usd`, `duration_seconds`). Production statuses (`pending/running/completed/failed/cancelled/archived`) are pinned in `jetty_contracts`; captured redacted responses live in `tests/fixtures/jetty/` under `tests/test_jetty_live_contract.py`; the opt-in live smoke is `RUN_JETTY_SMOKE=1` (`tests/test_smoke_jetty.py`). Submission retries now cover 429 only, since a 5xx can arrive after the workflow starts and a resubmit would double sandbox spend.
- Native Codex cleanup: observe CLI-leader exit separately from inherited capture-pipe EOF; on POSIX, terminate remaining members of the native CLI's process group before removing each isolated `CODEX_HOME`; retry transient `ENOTEMPTY`/`EBUSY` removal races with bounded backoff; bound or abandon pipe draining when an escaped process keeps capture descriptors open; and preserve completed answers while recording any cleanup recovery or fallback in stderr and environment artifacts.
- Evidence boundaries: add a typed four-axis observation contract for process exit, provider response, trace, and committed artifact set. Full-run operation evidence exists only when process, provider response, and trace are independently complete; callers cannot overwrite derived evidence fields. New answer and Jetty run directories carry a digest inventory in `artifact-commit.json`, written last and verified when artifacts are read.
- Answer experiment identity: current answer runners persist the exact case/model/repetition design and eval-contract digest in `answer-design.json`; each run attests its task and instruction. Benchmark, comparison, slice, reliability, cost, and causal-ablation headlines remain unavailable when expected arms are missing, duplicated, stale, or only partially graded. Blind-comparison results now require an explicit successful lifecycle and the matching answer-design digest.
- Closed eval inputs: assertion objects reject unknown or type-inapplicable fields; `golden_output` references must resolve to regular files at validation; local script oracles bind their containing oracle tree into the eval contract and reject symlinks; JSON boundaries reject duplicate keys, ambiguous multiple verdicts, and non-finite numbers. This intentionally requires regeneration of stale prepared/comparison artifacts and correction of previously ignored manifest typos.
- Fail-closed adapters: malformed zero-exit Claude envelopes and Vibe streams without a final assistant message remain diagnostics rather than candidate answers; actual subprocess return codes are preserved separately from provider-response failure. Failed Jetty trajectories cannot certify operation telemetry, and empty legacy `events.json` files no longer count as command evidence.
- Judges, Jetty, and budgets: judge verdicts bind to the exact prompt, candidate, context, evidence mode, and completed provider lifecycle; incomplete panels and grading keep only explicitly labelled observed diagnostics. Malformed new judge output now always fails closed; the accepted `--strict-judge-schema` and manifest `judge.schema_enforcement` controls are deprecated compatibility no-ops. Jetty imports validate the full batch and model-visible task/upload contract before atomically committing any destination. Incomplete judge-panel cost is a partial known subtotal rather than complete spend; suite budget history is accepted only when v3 scalar totals, aggregate values, and coverage counts agree.

## v0.6.0 — 2026-07-10

- Telemetry: keep successful provider completion separate from trace completeness. Provider-reported usage and cost remain available when a CLI returns no trace, while tool/command/file/error/retry/skill-invocation measurements stay unavailable instead of becoming false zeros. The live release smoke now checks this schema-v3 invariant.
- Documentation: add the 0.5.1 → 0.6.0 harness upgrade runbook. It separates package, manifest, and telemetry migration; preserves the old run tree before mutation; identifies strict pair/judge/trigger/Jetty/Codex compatibility boundaries; explains expected report-number changes; and gives a concrete rollback path.
- Correctness by construction across the remaining core domains: validate exact `(case, model, repetition, population)` experimental pairs before lift/reliability/cost/token arithmetic, slice/case/readiness comparisons, and answer-ablation confirmation (symmetric assertion coverage plus per-pair sign-flip significance); reject duplicate arms and expose blocked-pair diagnostics; replace the mutable answer-runner result bag with frozen `Completed | TimedOut | SpawnFailed | ProviderFailed` states; parse imported judge rows into strict boolean/scored/dimension/dynamic/consensus verdicts while rejecting duplicate IDs and contradictory pass/score/threshold fields; split permissive `PreparedTaskDraft` from validated executable `PreparedTask`; parse Jetty into queued/running/succeeded/failed/timed-out/protocol-invalid lifecycles, where completed-without-`output.md` is protocol-invalid; count only typed-completed trace operations; and close ablation mode/population/component/mechanism vocabularies. Exhaustive state tables, model-gap tests, mismatched-pair integration tests, and provider-shaped fixtures cover each boundary.
- Correctness by construction: replace independent trigger-run booleans with frozen `InvocationOutcome`, `PiStream`, `TriggerDetection`, and `TriggerObservation` contracts; classify exit-zero Pi provider/protocol failures before trigger scoring; consume Pi cumulative usage once; revalidate persisted trigger rows in the live smoke; and centralize capability-qualified smoke model targets. Sanitized Pi lifecycle fixtures and exhaustive finite-state/truth-table tests guard the boundary.
- Documentation: a docs consistency audit reconciled twelve confirmed defects — an inverted leak-saturation polarity in `why-did-this-run-fail.md`, `authoring-evals.md` redefining "saturated" as both-arms-pass (the glossary's base-saturated), the glossary citing a nonexistent `eval-readiness` command and a wrong `eval_intent` value (`regression`, not `regression_guard`), a conflated usage/cost `source` vocabulary, the stale Pi-only story about who measures discovery ablations (the trigger matrix does too, with any adapter), two docs using `RAW_MEASUREMENT` for paired comparisons the `EvidenceClass` enum labels `INDETERMINATE`, `gating-ci-on-evals.md` overstating `--fail-on-blockers` (missing hidden splits are a `required` finding, not a blocker), a four-vs-five docs-kind miscount, and two unrowed index entries (`materialize-ablations`, `docs/README.md`). The registry conformance guard now also fails when a registered backend is absent from `docs/agent-parity.md`.
- Documentation: add the porting journey `docs/porting-existing-evals.md` (arriving from another framework: `dataset_files` + one template case as the mechanical seam, then the paired baseline/splits/leakage discipline the source framework had no slot for, with real offline lint/lift/audit output and the `audit-manifest` findings as the post-port punch list — this empties the user-journey backlog), extend `docs/vocabulary.md` with a "Runners and judges" section (agent backend, workspace isolation, judge backend, judge task, repetition/panel, judge alignment, judge robustness) plus `depends_on`, eval-intent, readiness, reliability (pass@k/pass^k), and contamination entries, and sweep confirmed drift: the CLI `--help` epilogue now names `run-agent`/`--judge-backend`, `docs/agent-parity.md` joins the README documentation map, the README's TODO summary covers the agent-parity/migration backlogs, and two stale TODO entries are reconciled (shipped Jetty-docs marking ticked; the `--judge-cmd` escape hatch reframed as a standing invariant).
- Packaging: raise the declared build floor to `setuptools>=77` — the PEP 639 SPDX `license = "MIT"` string fails to build on the previously declared `>=68`.
- Hardening: consolidate native subprocess handling so spawn failures become failed observations instead of crashes, isolate the Pi trigger runner cwd like the trigger matrix, validate trigger eval-set booleans at the JSON boundary, and add capability/publish-workflow conformance guards.
- Documentation: reconcile stale ablation, evidence-class, assertion-family, agent-parity, and demo walkthrough prose with the current materialized-ablation/significance behavior.
- Packaging: add a PyPI trusted-publishing workflow (`.github/workflows/publish.yml`) for tokenless release uploads from GitHub Actions, publish `skill-eval-harness==0.5.1` to PyPI, and update README install examples to prefer PyPI.
- Native agents: add first-class Mistral Vibe answer/judge/trigger support, harden Codex/Vibe CLI homes so credential-bearing files live outside model workdirs, and document current Vibe telemetry/schema gaps.
- Documentation: add the judge-calibration journey `docs/can-i-trust-my-judge.md` (`judge-robustness` negative controls → `judge-alignment` vs. human labels with Cohen's kappa → `compare-judges` lift sensitivity), backed by the demo's first qualitative assertion (`actionable-review`, gate severity) and a deterministic `stub_judge.py` whose `--lenient` rubber-stamp mode exists to be caught (kappa 0.0 at 0.5 raw agreement, `control_leak_rate` 1.0, measured lift eroded 1.0 → 0.83); the signature is pinned by `DemoJudgeTests` and the plain no-judge benchmark output is unchanged.

## v0.5.1 — 2026-07-08

- Packaging: add an explicit setuptools build backend (`[build-system]`) so PyPI/PEP 517 builds do not rely on implicit backend defaults.
- Documentation: make the README's user-facing questions explicit, restructure the v0.5.0 changelog for scanability, and normalize GitHub release titles to version numbers.
- Roadmap: track first-class Gemini CLI and Mistral Vibe backend support in `TODO.md`, including answer runners, judge backends, autonomous trigger adapters, smoke-test env vars, and offline conformance fixtures.
- No runtime behavior changes from v0.5.0.

## v0.5.0 — 2026-07-08

### Release summary

v0.5.0 is the first native multi-backend release. It moves the harness from a mostly runner/import/export workflow to a shared agent-backend path with native Claude and Codex answer runs, native Codex judging, stricter subprocess safety, and a larger release-grade eval surface.

| Area | What changed |
|---|---|
| Native runners | Added `skill-benchmark run-agent --agent claude|codex`; `run-claude` and `run-codex` remain compatibility wrappers over the shared path. |
| Native judging | Added `judge --judge-backend codex`, reading verdicts from `--output-last-message` and treating Codex stdout JSONL as telemetry. |
| Safety by construction | Native subprocesses require an explicit cwd, default to isolated workspaces, write failure artifacts on spawn errors, and kill process groups on timeout. |
| Structured verdicts | Codex/OpenAI-facing schemas are adapted to strict structured-output requirements while returned verdicts still validate against the canonical harness schema. |
| Eval framework surface | Materialized ablations, model-axis reporting, cost telemetry, trigger matrices, judge robustness/alignment, contamination checks, CI reports, viewer improvements, and the offline demo-skill workflow are included in this release line. |
| Docs/specs | Added the agent backend interface spec and expanded command/parity docs for Claude, Codex, and future Gemini/Vibe-style adapters. |

### Validation

- GitHub CI passed on Python 3.10, 3.11, and 3.12 for the release commit.
- Local full suite passed: 607 tests OK, 4 skipped.
- Strict demo eval-quality gate passed: `validate --strict-leakage --leakage-min-chars 1 --check-ablations`.
- Demo suite smoke tiers passed: `preflight`, `static`, `prepare`, and `jetty-dry-run`.
- Package artifacts built and passed `twine check dist/*`.

### Known caveat

- Claude live runs can still fail under local Claude quota/auth limits; v0.5.0 records those as non-scorable failure artifacts instead of grading quota text as candidate answers.

### Detailed changes

- **Native Claude/Codex parity path.** `skill-benchmark run-agent --agent claude|codex` is the shared native answer-runner path; `run-claude` and `run-codex` are now compatibility wrappers. Claude runs execute in the isolated task workspace and treat `claude -p` JSON envelopes with `is_error:true` (for example quota/429 responses that exit 0) as non-scorable infrastructure failures instead of grading the quota text. Codex answer runs use a config-light isolated invocation (`--skip-git-repo-check --ephemeral --ignore-user-config --ignore-rules --sandbox read-only -`) and keep trace-derived token telemetry. `judge --judge-backend codex` adds native Codex judging via `codex exec --output-last-message --output-schema`; stdout JSONL is telemetry, not the verdict stream, and the provider-facing schema is adapted to Codex/OpenAI structured-output requirements while returned verdicts still validate against the canonical harness schema. Native subprocesses now require an explicit cwd by construction, write failure artifacts on spawn errors, and use process-group timeout cleanup. Docs, capability rows, and parity spec updated; live good-pr smoke artifacts under `/tmp/skill-eval-agent-parity-good-pr-20260707-204946` show successful Codex answer/judge coverage and fail-closed Claude quota handling.
- **Three more user-journey walkthroughs, closing the "code supports it but no doc walks it" backlog for the iteration/debugging loops.** `docs/did-my-skill-edit-regress.md` (the edit → re-run → diff loop: the within-run `ablation_regressions` block as the assertion-level, significance-gated before/after, and `render-viewer --previous-workspace` diffs across the `iteration-N/` convention as the cross-iteration localizer; `compare-results` was evaluated and rejected — it tallies a pairwise-preference judge, not an assertion diff), `docs/which-model-should-my-skill-target.md` (`prepare --models` fan-out → the `by_model`/`model_analysis` blocks → reading real per-tier lift against base-model saturation; the offline stub is model-blind so all tiers come out identical, which the doc names as the honest plumbing-only shape), and `docs/why-did-this-run-fail.md` (debugging one failing run: the `error-analysis` taxonomy + open-coding review queue, then the run dir's `output.md`/`metadata.json`, mapped to a failure class and a manifest-or-skill decision). All three are runnable offline on `examples/demo-skill` with real 2026-07-06 output, added to the README documentation map, the `docs/README.md` journey table, and ticked in the `TODO.md` backlog. `LESSONS_LEARNED.md` gains two 2026-07-06 lessons distilled from the consolidation and trigger-matrix work (consolidate a drifting concept to one owner and guard the fork; treat autonomous activation as its own per-(agent, model) measurement) plus a doc-link-guard annotation on the prose-to-code-references lesson.

- **One runner-outcome writer.** The answer runners (`run-codex`, `run-claude`, `run_subagent_tasks`) each hand-rolled the adaptation of their raw provider result onto the on-disk run contract (`output.md`/`metadata.json`/`events.json`/`metrics.json`/`trace.jsonl`), and the copies had drifted — the Codex empty-output path wrote metadata *without* the normalized usage/cost blocks the others always wrote. Each runner now does only provider-specific work (spawn the tool, parse its wire format) and returns a typed `ablation_model.RunnerOutcome`; one shared `write_runner_outcome` adapts it, delegating trace/events/metrics/metadata to `write_trace_artifacts` so telemetry precedence, the current-run-only metadata guarantee, timeout encoding (`timed_out: true` + returncode 124), and failure-body marking have a single owner. Grading and pass/fail are unchanged; the deliberate consistency effects are on the run artifacts (all runners now emit one shape): every answer runner's `events.json`/`metrics.json` carry `schema_version: 2` and the trace-normalizer's metric keys (`tool_calls`, `skill_invoked`, …) even for an empty trace — previously Codex's empty-output path and every Claude run wrote `schema_version: 1` with none of those keys, while the subagent already wrote schema-2. Telemetry is always an explicit block (`{"source": "missing"}` when absent, previously omitted on the Codex empty path). `metadata.json` gains whatever `metrics.json` carries plus `stderr`/`model` and (on the empty/timeout paths) an `environment.json`, so Claude/Codex metadata pick up keys they lacked. `anthropic_grading_json` therefore records `execution_metrics.tool_calls: 0` (was `{}`) for a traceless run. No downstream rate, cost total, or verdict changes — the added keys are zero/empty and readers tolerate them — but a schema-aware artifact consumer sees the uniform shape. The provider→failure-marker binding lives once in `RUNNER_FAILURE_MARKER_BY_PROVIDER` (subagent reuses `[CLAUDE FAILURE` — its backend is Claude — and the subagent seam keeps diagnosing via `error`/empty rather than a returncode+stderr body). Guards: `test_runners.py` pins that Codex and Claude produce the same contract shape and covers the writer's timeout/missing-telemetry/derived-answer paths; `test_consolidation_guards.py` source-checks that no answer runner hand-rolls a run-level contract write.

- **Docs restructure: moat-first README, command reference split out, one-definer concept docs.** The README now opens on the causal-lift differentiator (paired with/without, split discipline, leakage lint, materialized ablations, per-model lift) instead of "a narrow question," and elevates activation and cost telemetry to first-class `What the CLI owns` bullets. The ~440-line inline command walkthrough moved to `docs/commands.md`; the README keeps a grouped command index (every subcommand still named, guarded by the doc-coverage test). The `Ablations` section now sits above the command index. Concept docs adopt one definer per term: `vocabulary.md` is the canonical glossary and `abstractions.md` / `academic-grounding.md` / `evals-are-not-tests.md` declare themselves as the engineering / research / interpretation lens and link to it rather than redefine — enforced by a new `ConceptDocConventionTests` guard. Two new user-journey walkthroughs land: `docs/is-my-skill-worth-its-tokens.md` (footprint vs. lift-per-dollar) and `docs/gating-ci-on-evals.md` (the two-gate CI recipe), both runnable on `examples/demo-skill`. A second new guard, `test_doc_links.py`, resolves every relative doc link — file and `#anchor`, fence- and inline-code-aware — so a section move can't strand a link.

- **Provider-neutral workspace/prompt helpers.** `codex_skill_workspace` and `codex_task_prompt` are renamed to `build_skill_workspace` and `build_task_prompt`. They were never Codex-only: `run-codex`, `run-claude`, and `run-subagent` all mount their isolated workspace and build their task prompt through the same two helpers (all three register `build_skill_workspace` as their workspace builder). The names now describe the behavior instead of one caller. Pure rename — no behavior change.

- **Consolidation audit, part 2: the deferred structural refactors.**
  - *One discovery loop.* `discovered_run_units` now owns the case/model/variant/run walk for `grade`, `benchmark`, the judge collector, and the contamination report (four hand-synced copies before). The suite ledger's disk walk is the named `discover_on_disk_run_rows` beside it — the ledger deliberately bills every on-disk arm while grading is variant-scoped, and that difference is now a documented decision instead of two private implementations.
  - *One cost rollup.* Both ledgers (and the manifest audit's ablation-spend finding) build coverage/totals/spend groups from shared `spend_of`/`group_spend`/`cost_coverage_block`/`cost_totals_block`/`judge_cost_block` helpers.
  - *One timeout encoding.* Every spawned process now records a timeout as `timed_out: true` + returncode 124: `run-codex` and `run-claude` (previously `returncode: null`), `run_subagent_tasks` (previously a generic error that LOST the timed_out flag and mislabeled the failure), `shell_agent_backend`, `suggest-cases` (previously a raw traceback), and `suite-run` tier commands (previously unbounded). `DEFAULT_RUNNER_TIMEOUT_S` replaces eight duplicated `1800` literals; a timed-out subagent output now carries the `[TIMEOUT` marker.
  - *One `ablation:<id>` reading.* `ABLATION_VARIANT_PREFIX`, `is_ablation_variant()`, and `ablation_id_of()` in `ablation_model` replace 14 inline `split(":", 1)[1]`/`startswith("ablation:")` sites.
  - *One section scan.* `_locate_section` owns the fence-aware, level-exact markdown heading scan behind `section_span` and `list_item_ops`.
  - *Tests reorganized by subject.* The PR-named files (`test_audit_fixes`, `test_followup_features`, `test_roadmap_features`, `test_external_review_gaps`, `test_cbc`) are gone: their classes moved verbatim (docstrings citing finding/roadmap ids preserved) into `test_manifest.py`, `test_grading.py`, `test_judging.py`, `test_reporting.py`, `test_stats.py`, `test_runners.py`, and `test_ablations.py` — the last also absorbing test_skill_benchmark's 1,900-line ablation half. One strict-subset duplicate test was deleted rather than moved. Sibling-TestCase borrowing (`SubagentRunnerTests().make_tasks(...)`) is gone: shared builders are module-level or in `tests/helpers.py` (which now also owns the good-pr fixture family, the demo manifest, and the report fixture). New guards pin the refactors: discovery/rollup/timeout owners are source-checked and the timeout convention is behavior-tested.

- **Consolidation audit (testing-best-practices)** — a duplication/consistency audit of the whole project, with fixes and executable guards against recurrence:
  - *Trigger runners share the harness's owners.* `skill-pi-trigger-eval` and `skill-trigger-matrix` had re-forked repo-root resolution, manifest loading, skill-tree mounting, the Pi argv, and subprocess-timeout handling — with real behavioral drift (a manifest outside `evals/` mounted from a different root than `benchmark` used; YAML manifests and `dataset_files` worked in the harness but silently broke in the runners; dataset-templated trigger cases were invisible to both runners). They now import `repo_root_for_manifest`, `load_manifest_source`, `iter_cases`, the new `mount_skill_tree` and `run_argv_with_timeout`, one shared `pi_argv`, and one shared `--eval-set` loader. The report-level evidence-class string is owned by `ablation_model.TRIGGER_MEASUREMENT_EVIDENCE_CLASS`. `run_pi_trigger_eval --runs-per-query` now defaults to 3 (the floor `docs/tuning-skill-activation.md` documents; it was 1). The matrix's frontmatter regexes are replaced by the harness's real `frontmatter_value` parser.
  - *One token-usage alias table.* `usage_number` (trace streams) and `CLAUDE_USAGE_KEYS` (the `claude -p` envelope) had their own drifted copies of `USAGE_ALIASES`, so the same provider payload could be classified differently by two paths. Both now read the one table (which also learned `cached_input_tokens` → cache-read and bare `tokens` → total).
  - *One population boundary.* `benchmark` skipped `kind: "trigger"` cases but `grade` and the judge collector did not — they would happily grade/spend judge calls on discovery-population outputs. All graders now route through a shared `is_trigger_case`; readiness and the manifest audit share `is_judge_only_case` (they previously disagreed on rubric/factuality-only cases).
  - *One report-writing tail.* Every reporting command now emits through `emit_report` (parents created, trailing newline) — `contamination`/`judge-robustness --out new-dir/x.json` used to traceback where `benchmark` succeeded. Judge verdicts and comparison verdicts load through one `load_result_rows` parser (a pretty-printed JSON array used to load in one command and crash the other). Both cost ledgers read judge spend through one `judge_cost_usd` (a verdict whose cost lived only in `cost_normalized` counted in one ledger and not the other).
  - *Dead weight and small dedup:* `detect_trigger` drops its never-read `skill_name` parameter (each caller computed a divergent value for it); `num()` folded into `metric_number`; `anthropic_grading_json` reuses `expectation_texts`; `ablation_by_id` replaces five inline lookups; `iter_json_objects` replaces five hand-rolled JSON-stream scan loops; `JETTY_DEFAULT_BASE_URL` hoisted. `main()` split into `build_arg_parser()` (all three CLIs) so tests can enumerate the surface.
  - *Examples:* `generate_shared_manifests.py` deleted — it was inert (wrong `ROOT` after the workspace move) and generated the exact meta-prompt-trigger and leaky-assertion anti-patterns `docs/authoring-evals.md` prohibits. `smoke_report.py` no longer re-implements (and had drifted from) the benchmark report's flag vocabulary; it delegates to `build_benchmark_report`.
  - *Test suite:* new `tests/helpers.py` (single owners for the eval-repo builder, run-dir writer, result rows, judge tasks, the `claude` stub) — the suite carried ~25 drifting copies of the repo builder; the worst named duplicates now delegate. `skill_benchmark` is no longer executed **twice** per test session (two files loaded it via unregistered importlib specs, giving two module instances with divergent registries) — everything imports the one instance, `run_pi_smoke` loads through a registered shared loader. `Provenance.SCHEMA_KEYS` replaces six copies of the key-set literal; seven leaked temp dirs now clean up; a hand-rolled `statistics_mean` is gone.
  - *Guards against recurrence* (`tests/test_consolidation_guards.py`, per testing-best-practices doc-sync-testing): identity tests pin that runner helpers ARE the harness's functions; every CLI subcommand and assertion type must be documented in the README (this immediately caught `contamination`, `judge-robustness`, and `cost-summary` missing — now documented); the README may not document phantom assertion types; every `--split` flag must offer exactly `VALID_SPLITS`; the evidence-class literal may not be re-spelled; every grading path must route the trigger boundary through `is_trigger_case`. `tests/test_doc_refs.py` now also scans README/CHANGELOG/CONTRIBUTING/LESSONS_LEARNED.
  - *Docs reconciled:* stale line references updated (enforced), shipped work un-open-itemed in `trace-aware-eval-spec.md` (leakage-lint expansion, judge-verdict schema validation; `metrics.json` example now `schema_version: 2`) and `academic-grounding.md` (unbiased pass@k / pass^k shipped), the duplicated Jetty live-token checklist in `TODO.md` replaced by a pointer to the spec's copy, `repo-effectiveness-audit.md` no longer claims zero runtime dependencies or a fixed test count, the demo README no longer routes `claude -p` through `run-codex`, and the bug-report template placeholder is current.

- **Activation tuning journey**: new `skill-trigger-matrix` command (`run_trigger_matrix.py`) measures autonomous skill activation per (agent, model) cell — the skill mounts where the agent discovers skills on its own, never force-loaded, and each cell reports trigger rates split by should-fire/should-not-fire polarity. Ships three adapters: `claude` (headless Claude Code subagents, defaulting to the haiku/sonnet/opus aliases, with `Skill`-tool-by-name detection plus the shared path-evidence fallback, and a fresh `CLAUDE_CONFIG_DIR` per run so personal skills stay out while the agent's built-ins stay in), `pi` (same mount/detect as `skill-pi-trigger-eval`, which remains the deeper Pi tool), and an offline deterministic `stub` that decides from the mounted description — so a weakened description measurably under-triggers even in CI. Other agents (Codex, …) register via an `AgentAdapter` subclass. The walkthrough is `docs/tuning-skill-activation.md`; `examples/demo-skill` gains one trigger case per polarity; `tests/test_trigger_matrix.py` covers the pipeline offline and wraps a manual live smoke (`RUN_TRIGGER_SMOKE=1`). `TODO.md` gains a "user journeys the code supports but the docs don't walk" backlog from the same review.

- **Multi-agent audit hardening** — a four-lens adversarial audit (correctness, safety-by-construction, invariant preservation, test strength) of this session's work surfaced fixes, each landed with a regression test:
  - **G2 order-dependent veto (correctness, was the highest-severity finding)** — `grade_case_variant`'s staged-grading post-pass keyed its resolver on the emitted row *name*, but a preset assertion rewrites that name (e.g. `factuality`) while `depends_on` targets the author's label (e.g. `grounded`). A forward reference to a preset prerequisite therefore failed to skip and could spuriously **veto the whole run** — and the outcome depended purely on assertion *order*. The resolver now keys on a stable original-assertion label (stamped transiently, stripped before serialization), so a dependent skips identically in any order. Reproduced then fixed.
  - **Tool-using judge cwd (safety)** — the judge subprocess inherited the harness cwd (the repo root, which holds the live `runs/…/grading.json`), so its read-only tools could range over the oracle regardless of the sanitized copy. The explore judge now runs with the **sanitized copy as its cwd**.
  - **Symlink dereference (safety)** — `sanitized_run_copy` used `copytree`'s default `symlinks=False`, which *follows* a link and copies the target's content under the link's (innocent) name — smuggling an oracle past the name denylist. Symlinks are now dropped from the copy entirely.
  - **Empty `run_base` (safety/correctness)** — a judge task missing `run_base` let `Path("")` resolve to `.` (the repo root), so `--judge-explore`/`--judge-trajectory` would copy or inventory the whole repo. Both paths now require a real run dir and degrade to output-only without one.
  - **`released_at` gate ordered lexically (contamination)** — `"2024-6" <= "2024-12"` is false as strings, so a pre-cutoff case wasn't flagged. Now compared as parsed `(year, month, day)` tuples (`cutoff_key`), release-earliest vs cutoff-latest, so mixed-precision stamps order by date.
  - **Consensus tie coin-flip (G3)** — an even panel tie with no calibrated threshold resolved via `median >= 1` (the default), i.e. almost always PASS. A bare raw-score tie now resolves to `unresolved` unless an explicit threshold decides it.
  - **Manifest `judge.panel`/`judge.models` validation** — the consensus activation surface is now shape-validated like every other manifest field.
  - Plus test-strengthening across the session's features (median-not-mean, quorum/threshold boundary cases, unanimous-all-fail, reverse-order transitive chains, skipped-soft/qualitative exclusion, trajectory-payload-not-hint, reserved-file inventory) — mutation-killers for gaps the audit found by running the mutations against a green baseline.

- **Tool-using judge (G1 follow-on)** — `judge --judge-explore` lets a native judge *explore* the run directory with read-only tools (`Read`/`Grep`/`Glob`/`LS` — never Write/Edit/Bash) instead of only reading a prompt-embedded trajectory, the Agent-as-a-Judge pattern. The security property is **safety-by-construction**: `sanitized_run_copy` copies the run dir with every oracle file removed — anything whose name carries a leak marker (`grading`/`answer`/`rubric`/`expected`/`gold`), files *and* directories, recursively — and the judge is pointed (`--add-dir`) at that copy, never the live run dir, so a filesystem-reading judge physically cannot read the grader's answer key: it is not on disk to be read. Reserved files (`output.md`/`events`/`metrics`) stay, since a tool-using judge legitimately reads them. Requires `--judge-model`/`--judge-panel` (rejected with a shell `--judge-cmd`); the sanitized copy is scratch and cleaned up after each task; byte-identical judge invocation when off. The core grade path is untouched and still model-free.

- **Judge-robustness probes** (from the awesome-evals backlog) — a new `judge-robustness` command that stress-tests the *judge itself* rather than the model under test: an **order-flip self-consistency** check (`flipped_judge_task` reverses the rubric / expected-behavior / graded-dimension ordering — a verdict that flips is position-sensitive, a Zheng-et-al. bias) and two **negative controls a robust judge must reject** (`JUDGE_NEGATIVE_CONTROLS`: an empty candidate, and a "master-key" prompt-injection that orders the judge to output PASS). A finding fires when the verdict flips under reordering or when the judge passes a control it should fail; the summary reports `order_flip_consistency` and `control_leak_rate`, and `--fail-on-findings` gates CI. Model-touching, so it runs only under this opt-in command behind `--judge-cmd`/`--judge-model` — the core grade path stays local, deterministic, and model-free. Sibling to cross-judge consensus (G3): shares the judge plumbing, measures stability rather than combining judges; distinct again from `compare-judges` (metric divergence) and `judge-alignment` (accuracy vs human labels).

- **Contamination perimeter, output side** (from the awesome-evals backlog) — a new `contamination` command plus pure detectors that see the *output*, complementing the prompt-side leakage lint: a **canary-GUID tripwire** (a case's `canary` string appearing in an output means the model has seen the held-out eval), **output↔answer n-gram containment** (`ngram_containment` — the fraction of the answer key's word n-grams reproduced verbatim, a memorization signal), and a **`released_at` / `--model-cutoff` gate** (a case released at/before the model's training cutoff may be contaminated). Optional `canary`/`released_at` case fields (validated), `--fail-on-contamination` for CI. Model-free and deterministic.

- **G2 inline judge-call suppression (deferred optimization, now landed)** — a dependent whose case-level prerequisite has already resolved as failed is skipped *without evaluating*: no judge task is emitted and no script oracle runs for it (extending the existing scorable-run judge-call guard). The post-pass stays the authoritative resolver (forward references, deferred qualitative prerequisites), so counts and pass rates are unchanged — the existing G2 count-tests are the regression guard — while `deferred_judge_tasks` no longer grows for a skipped dependent.

- **Cross-judge consensus (G3)** — `merge_cross_judge_rows` folds a panel of ≥2 per-model judge verdicts into ONE consensus verdict of the same shape (majority `passed`, median `score`, joined evidence), so the benchmark join is untouched. `judge --judge-panel m1 --judge-panel m2` (or a manifest `judge.panel`) resolves via `effective_judge_models`, and `judge_command` becomes a two-level merge — repeat-merge kills within-judge noise per model, then consensus-merge kills across-judge bias. An even tie resolves to `unresolved` (never a silent coin-flip) unless the score median crosses the threshold or `--quorum k` decides it; an `agreement` block records per-task concordance. Panel cost is summed onto the single top row (members nested under `judge_panel`) so the judge ledger doesn't double-count, and the judge-is-model-under-test guard now checks every panel member. Deliberately distinct from `compare-judges` (per-report metric divergence) and `judge-alignment` (accuracy vs human labels); a 1-member panel short-circuits to the single-judge verdict, byte-identical. Sibling to the open judge-robustness-probes item (shares the merge plumbing, not the same feature).

- **Assertion dependencies / staged grading (G2)** — an optional per-assertion `depends_on` (a prerequisite label or list). When a prerequisite fails or is itself skipped, the dependent is **skipped** — dropped from every pass-rate denominator, the soft graded mean, the reference floor, and the critical veto — rather than counted as a second failure (skip, not zero, keeps denominators honest). A post-pass over the graded rows resolves it, iterated to a fixed point so transitive chains (A→B→C) all skip and a deferred **qualitative** prerequisite resolves on the verdict-loaded pass. Critical-tie correctness: a `severity: "critical"` dependent whose prerequisite failed is *skipped*, so a never-run critical assertion cannot collapse the run — the veto stays owned by the prerequisite's own severity. Validated at manifest load: shape, target-exists, label-uniqueness, and cycle detection (a self-dependency is a 1-cycle); rejected in turn assertions. Additive — a manifest with no `depends_on` grades byte-identically (`skipped_total: 0`). Ships the authoritative post-pass resolver; the inline judge-call-suppression optimization is a follow-on.

- **Run-dir / trajectory judge (G1)** — an opt-in `--judge-trajectory` hands the judge the run's normalized `events.json`/`metrics.json` (the same trajectory the process/efficiency detectors read) plus an artifact inventory, so it can weigh *how* the answer was produced, not only the final `output.md` text (the Agent-as-a-Judge gap — black-box judging plateaus). The inventory is a **denylist**, not a bare walk: `judge_artifact_inventory` excludes `grading.json`, answer-key/rubric files, and the reserved output/events/metrics files, so a blind judge can never read the grader's oracle out of a run dir that `--write-grading-files` populated. Default off ⇒ the judge prompt is byte-identical, the payload additions are opt-in, and core grading stays model-free; a malformed `events.json` degrades to text-only rather than crashing. Ships prompt-embedded trajectory only — a file-exploring tool-using judge is held.

- **Capability-vs-regression intent (G5)** — a per-case `eval_intent: capability | regression` axis. A *capability* probe (the default) that saturates or shows no lift is a wasted-budget blocker as before; a *regression* guard is meant to sit green, so its saturation/no-lift is the intended steady state — `readiness_run_signals` routes it to a new non-blocking `regression_guards_holding` instead of `base_saturated_cases`, so `audit-manifest --fail-on-blockers` stays green, `trend` staleness never proposes it for pruning, `suggest-cases` never seeds a harder variant from it, and the `saturated-eval`/`no-lift-eval` audit findings are suppressed (flaky/variance stays `required` for both). Optional and strictly validated; every untagged (v1/v2) manifest defaults to `capability` and grades and gates byte-identically. Orthogonal to `case_polarity` and to ablation `expected_regressions` (per-ablation, not per-case).

- **Schema-constrained judge output (G4)** — every judge verdict now has a canonical JSON Schema (`verdict_schema_for`, one per plain / `graded_dimensions` / `dynamic_rubric` shape), embedded in the judge prompt as the contract and validated post-hoc with the existing `json_schema_errors`. A well-formed-but-wrong-keys verdict (no `passed`, a graded verdict with no `dimension_scores`) that `extract_json_object` used to accept and silently coerce to failed/None is surfaced in a `schema_errors` field and always fails closed. `--strict-judge-schema` and manifest `judge.schema_enforcement` remain accepted as deprecated compatibility no-ops. Validation is pure and local (no model call). Constrains the *judge's* output, distinct from `structured_output` which constrains the model-under-test.

- **Paired reliability lift (G6)** — the `benchmark` `reliability` block gains a `paired_lift` view: the `with_skill − without_skill` delta on unbiased **pass@k** (does the skill raise the ceiling — *ever* succeeds) and **pass^k** (does it raise reliability — *always* succeeds), per case and pooled per shared k, with a sign-flip permutation p-value on the pass@1 delta. `build_reliability` scored each arm alone; this closes the gap to a paired, significance-tested reliability lift, reusing the existing `sign_flip_significance` and `build_paired_summary` (case, model) pairing (so `by_model` reliability lift lines up with `paired_summary.by_model`). Additive: per-arm reliability keys unchanged, no new manifest field or CLI flag, closed-form and model-free. First of six external-review gaps (see `docs/academic-grounding.md`; the rest sequenced in `TODO.md`).

- **Audit hardening** of the follow-ups below (a multi-agent `testing-best-practices` review + adversarial verification). Correctness fixes: the ablation significance test now runs **per case** (runs of different cases aren't exchangeable, so cross-case pooling both faked significance for a stack of single-shots and flattened real regressions) — a regression confirms iff ≥1 exact case/model cohort clears a paired sign-flip p≤0.05, and a cohort needs ≥6 matched repetition pairs to reach it; `judge-alignment` F1 is computed from counts so a label-inverting judge reports `0.0` not `null`; the `tool_call` taxonomy (`required_calls`/`call_set`/`expected_no_call`) matches completed-call **tool names** exactly instead of a substring over the rendered command (a shell `cat readme` no longer satisfies `required_calls:["Read"]`), and `call_set` is a true multiset. Hardening: `validate` rejects malformed `tool_call` assertions (multiple selectors, non-list/empty selectors, non-bool `expected_no_call`, uncompilable `pattern`/`order`); Monte-Carlo permutation p-values use the `(b+1)/(m+1)` estimator so a sampled p is never an impossible `0.0`. Plus mutation-killing tests for pass@k/pass^k properties, permutation two-sidedness, the per-case OR combination, F1 zero-vs-null, `kappa_band` bands, and the tool_call name semantics.

- **Trustworthy-measurement follow-ups** (post-#22 assessment). (1) The ablation confirmation gate now requires **statistical significance across replicates**: an observed regression is confirmed only when a two-sided paired sign-flip test over exact repetition-level combined-score deltas clears p ≤ 0.05; an observed-but-underpowered regression downgrades to `INDETERMINATE` (never `REFUTED`), carrying its `significance` block and a "re-run more per arm" note. One run per arm ties at p=1.0, so a single-shot ablation can no longer confirm — the n=5 walkthrough lesson, enforced rather than eyeballed. (2) New `judge-alignment` command validates a judge against **human labels** (not another judge): agreement, **Cohen's kappa** with an interpretation band, and precision/recall/F1 over a shared-`judge_task_id` confusion matrix, with a min-labels stability warning — the accuracy check `compare-judges` (judge-vs-judge sensitivity) deliberately does not make. (3) `benchmark` reports a **`reliability`** block: unbiased **pass@k** (`1 - C(n-c,k)/C(n,k)`, not the biased `1-(1-p)^k`) and **pass^k** per (case, variant) from the repeated-run data, plus a pooled per-variant headline. (4) The `tool_call` assertion gains the **BFCL-style call taxonomy** — `expected_no_call` (irrelevance), `required_calls` (order-independent subset, extras allowed), and `call_set` (exact multiset, unexpected calls fail). (5) New `error-analysis` command emits an **open-coding review queue** (one row per failing/errored run, anchored on its first upstream failure) and an **axial failure taxonomy** (first-failures counted by category, plus the report's own case-flag histogram) — model-free, over a `benchmark.json`.

- Make **token and dollar cost first-class eval telemetry** (issue #21). Every runner path (Pi smoke, Pi trigger — which previously recorded no usage at all — Codex, Claude, subagent, the judge wrapper, and the Jetty importer) writes normalized `usage_normalized`/`cost_normalized` blocks with explicit provenance (`provider_reported` vs `trace_normalized` vs `missing`/`not_applicable`; missing is marked, never zero; provider blocks beat trace-derived ones). `benchmark`/`aggregate` gain a `cost_summary` ledger — operational totals over ALL runs including execution errors, per-variant mean/median/p90, paired cost deltas, ablation marginal cost and cost per confirmed regression, judge spend as a separate line. New `cost-summary` command writes the standalone suite ledger (JSON + markdown) with top spenders and spend-without-signal findings. `suite-run` projects spend before any model call (medians from `--cost-history` ledgers, static fallback) and gates on `--max-estimated-tokens`/`--max-estimated-cost-usd`, failing closed when a dollar cap has no estimate. `token-overhead` adds dollar deltas, lift-per-dollar, and the cost of saturated/no-lift pairs. `audit-manifest --runs` flags expensive saturated/no-lift/judge-only cases, high-spend unstructured ablation arms, and high-footprint low-lift skills above `--expensive-case-usd`.

- Implement the **eval-framework roadmap** (`docs/eval-framework-roadmap-spec.md`, issue #18) end to end. The confidence floor is executable: paired should-fire/should-pass fixtures for every objective detector with a registration meta-test (CF.1), one cross-runner `without_skill` isolation invariant over a workspace-builder registry (CF.2), re-grade idempotence (CF.3), and a no-subprocess/no-network guard on the core grade path (CF.4).

- **Graded measurement (2.2)**: three-tier assertion severity (`critical` vetoes the run and is excluded from every mean; `gate` carries the pass rate; `soft` feeds *only* the per-run graded score and never moves the objective/qualitative/combined pass rate anywhere — declare `severity: "gate"` on a judge assertion to keep it in the pass rate). Anchored `graded_dimensions` and `dynamic_rubric` judge shapes work end to end through the `judge` command (which persists the graded payload and derives its verdict through the same owner the merge uses). Reference-anchor floors, a sign-flip permutation `significance` block beside every paired delta, a paired `graded` channel, and `structurally-pass-but-forgettable`/`critical-failure`/`below-reference-floor` case flags. `--strict` promotes soft to gate. Binary version-1 manifests grade identically (regression-tested).

- **Model as a third axis (2.1, 3.2)**: `prepare --models a,b,c` fans rows per model with a model run-dir segment; grading discovers both layouts; `judge_task_id` carries the model on fanned runs so verdicts merge onto the right model (single-model IDs unchanged); reports gain `by_model`, per-(case, model) pairing, `model_analysis` (lift ranking + lift losers), and slice-lift concentration ratios.

- **New deterministic assertion surface**: `golden_output` (reference-file equality with explicit normalization and diff evidence, 1.6), `similarity` (difflib ratio, thresholded and scored, 1.4; `mode: "embedding"` behind the opt-in `--embed-cmd`, 4.1), `structured_output` (JSON-Schema subset, 1.1), `tool_call` (matches completed shell-command *and* normalized `type: "tool_call"` events, with order/count bounds, 1.1), graded `script` oracles (`{"score", "max_score"}` stdout line, 1.8), oracle-strength tiers with per-case strong-share reporting and a `weak-oracle-only` audit (1.7), and the `factuality` judge preset (1.1).

- **Runner layer**: `run-subagent` (2.7) — an in-process backend seam writing the full run contract, honoring row models and multi-turn sequences, registered into the CF.2 invariant — with keyed, versioned **tool replay** (`--tool-replay record|replay|strict|auto`, 2.3); OTel GenAI semantic-convention attributes on every normalized trace event and metrics block (schema v2; v1 files still grade, 2.4); held-out rubric discipline (2.7b) — a `held-out-rubric-leak` audit finding plus `qualitative_by_visibility` separating held-out from tune-visible scores over judge-carried signal only (deterministic soft objective checks like `similarity` never enter that view).

- **Authoring and scale**: dataset templates fan one case over a row set with per-materialized-case leakage lint (2.5); YAML manifests + JSONL `dataset_files` compile in memory to the same manifest shape (3.3); multi-turn `turns` cases grade per turn against a turn-indexed transcript with the final turn as the answer of record, with per-turn assertions validated by the same validator as case-level ones (3.1).

- **Reporting and lifecycle**: `report --format junit|github` for CI (1.2), where a JUnit `<testcase>` fails on objective *and* qualitative gate/critical failures (soft never flips a case); `render-viewer` artifact embedding, `--serve` with `feedback.json` capture (2.8), and `--previous-workspace` iteration diffs (2.9); `trend` — an append-only history with series, successive diffs, prevalence×severity failure ranking (2.6), and staleness prune candidates (1.9); `suggest-cases` turning saturation flags into harder-case candidates with strictly opt-in generation that never edits a manifest (2.10, 4.2).

- **Judge config slot (1.3)**: manifests declare `judge.model` (the `judge` command's default); `audit-manifest` flags `judge-is-model-under-test` (fatal under `--strict-judge`). Leakage findings and fixture recommendations now point at the authoring guide's rule (1.5).

- **Manifest version 2 + migration**: `validate` accepts versions 1 and 2; `skill-benchmark migrate` stamps the mechanical defaults (severity, oracle tiers, `graded?` markers), prints the diff and the judgment-call checklist (`--check` dry run), and `docs/migrating-evals.md` is the versioned agent runbook ending in an identical-pass-rates re-grade check.

- Docs code references (`name:line`) across TODO/specs are now verified by a unit test against the actual source, so they cannot silently rot again.

- Add a first-class **Claude runner** (`run-claude`) that executes prepared rows through `claude -p --output-format json`, parses the envelope in one place (`parse_claude_cli_json`), and records real `total_cost_usd` + token usage into each run's `metrics.json`. The benchmark report now totals `cost_usd_total` per arm (over scorable runs), so a paired eval reports actual dollars — the thing the Codex/Jetty adapters left the caller to reconstruct. `CLAUDE_FAILURE` joins the runner failure markers so a nonzero exit/timeout is non-scorable like the others.

- Make the **judge model a recorded, multi-valued dimension**. Every judge verdict is stamped with `judge_model`; `judge` can grade natively with `--judge-model` (the Claude adapter, capturing per-verdict cost) or any `--judge-cmd`, both producing one verdict shape. New `compare-judges` takes ≥2 judged reports (`--report name=path`) and flags **judge-sensitivity** — `sign_sensitive` when judges disagree the skill helps, `magnitude_sensitive` when the with−without lift spread exceeds `--magnitude-eps`. A single judge number is not reproducible across judge choice for a subtle skill; this makes that measurable.

- Extend the eval-**readiness** verdict with the signals a static manifest can't see: `objective_only_cases` (a behaviour case with no judge assertion can only measure objective compliance) plus, when run data is supplied, `base_saturated_cases` (measured `with_skill == without_skill` — a blocker: the case measures nothing) and `qualitative_only_cases` (objective flat but combined lifts — the skill's value is qualitative, which an objective-only eval would miss).

- Keep the **answer and discovery populations distinct in the report**. `benchmark` no longer folds `kind:"trigger"` cases into its paired answer pass-rate (a discovery measurement is a different population, graded with opposite polarity by the trigger adapter); it records `skipped_trigger_cases` and stamps `population: "answer"`, so an answer pass-rate can't be lined up against a Pi-trigger `raw_autonomous_trigger_measurement` as if they were the same metric.

- Fix two judge-path defects surfaced by a live multi-model run: the judge-result merge crashed on a `{"passed": true, "score": null}` verdict (an eager `dict.get` default evaluating `None >= 1`) — now a single-owner `judge_verdict_passed` guards it; and judge-task emission now honors the `scorable_run` predicate like every other report view, so the judge no longer spends model calls grading missing/failed runs (24→8 judge calls on a real 4-case run).

- Add an eval-**readiness** verdict to `audit-manifest` (`eval_readiness`): a compact, offline "is this eval worth paying to run?" summary — ablations materialized vs instruction-simulated, **leak-saturated cases** (every positive objective assertion's value already appears in the prompt, so the case can't tell skill from no-skill), adversarial coverage, judge-only cases, and an explicit `blockers` punch list. Surfaced in both the JSON and markdown audit output. `audit-manifest --fail-on-blockers` exits non-zero when any blocker remains, so a skill repo can CI-gate its eval suite at "worth paying to run" the same way it gates tests. It turns the scattered findings into one gate you drive to green before spending model budget.

- Add `docs/ablation-study-walkthrough.md` + `examples/skill-pins.json` — a worked ablation study across ten real skills, pinned to exact commit SHAs and the harness's `canonical_skill_tree_hash` so it reproduces against the evaluated versions **without vendoring** skill content (the repo stores only `repo`+`sha`+`tree_hash` pointers; fetch on demand). Records the replication lesson — re-running the three strongest single-shot signals 5× per arm refuted two of them — and a no-vendoring reproduce recipe whose tree-hash check is verified end-to-end. The slide-maker pin demonstrates the tree-hash catching a branch that advanced past the evaluated commit.

- Add `examples/demo-skill/` — a self-contained, offline end-to-end example and the harness's executable documentation. A tiny synthetic skill with two materialized ablations (`section` + `reference`) and a deterministic stub runner (`stub_runner.py`) that keys off the mounted skill content, so `prepare → run-codex → benchmark` confirms a distinct regression per ablation with no model or API key. `tests/test_example_demo.py` runs it in CI (with_skill 1.0 / without_skill 0.0; each ablation `expected_regression_confirmed` with verified provenance).

- Make `PreparedTask` the sole authority after the JSONL boundary. The Codex runner (`codex_skill_workspace`, `codex_task_prompt`, `run_codex`) and the Jetty exporter (`jetty_task_name`, `safe_task_json`, `build_jetty_payload`) previously took a raw row dict and each re-derived the variant, skill paths, blinding, and upload token. They now take a `PreparedTask`, constructed once via `from_row` at each boundary (the `run_codex` loop and the `export-jetty` payload loop). The model-visible variant in a Jetty task JSON comes from `pt.model_facing_variant()` in one place (a blind materialized arm presents as `with_skill`; every other arm shows its true variant) instead of a per-variant override, so "model-facing variant / upload token / harness truth / skill paths" are owned by one object rather than re-spelled per adapter.

- Enforce the minimum provenance schema at the JSON boundary, not just for in-process construction: `Provenance.from_dict`, `Component.from_dict`, and `InstructionSimulated.from_dict` now raise `ValueError` on a missing, null, or wrong-typed required field (`id`/`mode`/`population`/`skill_hash`/`parent_skill_hash`/well-formed `components` for a Provenance; `class`/`mechanism`/`skill_root`/`target` for a Component) instead of silently constructing a `None`-filled record via `dict.get()`. An empty-but-present components list is still allowed. The regression verifier now catches a malformed recorded provenance and fails that confirmation gracefully (`provenance unverified: … malformed`) rather than crashing the report.

- Fix a mutation-before-validation ordering bug in ablation materialization: `_ensure_ablation_dir` created/cleared/marked the output directory before the containment gate (`_reject_output_root_overlap`) ran inside `materialize()`, so a bad `--ablation-dir`/`--out-dir` pointed inside a source skill root could write a `.skill-ablation-dir` marker into the live skill tree, and a harness-owned directory there could be cleared, before the command rejected it. `materialize-ablations`, `materialize_declared_ablations` (used by `prepare`/`export-jetty`), and `export-jetty`'s tree dirs now validate every declared ablation and run the non-mutating containment gates through one guarded `_ensure_ablation_dir_guarded` owner before anything on disk is touched.

- Drive the migration from instruction-simulated to materialized ablations: `audit-manifest` now emits an `ablation-instruction-simulated` finding for every label-only ablation (one with no `mechanism`/`components`), explaining that the arm is non-blind and raw-measurement-only and naming the structural mechanisms (`section`/`list_item`/`frontmatter_field`/`reference`/`patch`) that would make it a blind, confirmation-gradeable removal. Materialized ablations stay silent.

- Harden the ablation experimental contract: reject nested (ancestor/descendant) skill roots that would leave an unablated duplicate; confine frontmatter `patch` hunks to fields of the declared kind (discovery vs runtime); make `list_item`/`preprocess` honor Markdown structure (code-fence/inline-code/heading-level aware); restrict discovery (trigger-population) ablations to the autonomous-trigger adapter (`prepare` no longer emits them for forced-load runners); exclude infrastructure failures (nonzero exit/timeout/synthetic failure body) from scoring and confirmation; confirm a regression per cited case using the combined (objective+qualitative) score rather than OR-ing an objective drop across cases; verify exact recorded provenance on every measured run and require the canonical parent-tree hash to match on both arms before confirming; and give Jetty uploads variant-neutral names plus a runbook that never reveals the ablation arm.

- Make ablation runners sound: one canonical tree builder for every skill-bearing arm so `with_skill` and the ablation arm have an identical file surface (Pi smoke, `run-codex`, and Jetty all copy every root via the same copier). `run-codex` now executes in an isolated per-task workspace (the original repo skill is unreachable; `without_skill` mounts no skill). Full ablation provenance (`skill_hash`, components) is persisted in each runner's metadata. README routing/mechanism docs corrected.

- Add materialized skill ablations: `skill-benchmark materialize-ablations` writes real, altered skill trees (removal-only) for `manifest.ablations` that declare a `mechanism` or `components` + `target`. Mechanisms: `frontmatter_field`, fence-aware `section`, `list_item`, deletion-only `patch`, `reference` (pointer/content/both), `script`, `asset`; with multi-component composition and net-deletion / disjointness / layer-cohesion / required-field gates, unique slug ids, and path-traversal/containment checks. Manifests with no removal declaration keep instruction-simulated behavior unchanged.
- Wire materialized ablations through the runners and reporting: the Pi smoke runner mounts the altered tree, the Pi trigger runner gains `--ablation` to mount a materialized (e.g. weakened-description) skill, and `export-jetty` uploads the tree recursively with relative paths preserved (no basename flattening). `prepare`/`export-jetty` gain `--ablation-dir` (required when an ablation declares a removal); answer-population ablation rows route to non-trigger cases (discovery ablations go to the trigger adapter, not the generic runners) and carry `{mode, population}` provenance; and the benchmark report adds an `ablation_regressions` block distinguishing "score regressed" (aggregate) from assertion-level "expected regression confirmed".
- Complete the spec: `validate --check-ablations` dry-runs the apply-time gates (materializes to a temp dir, writes nothing); an opt-in `invalid_skill: true` ablation mode permits required-field removal and is reported separately; materialization records per-component `removed_bytes` + an `isolation_warnings` warning when one component removes >60% of a file; and `audit-manifest` flags ablation hygiene (dangling reference, unknown expected_regression case/assertion, missing expected_regressions).
- Add a `preprocess` removal mechanism (inline `` !`command` `` and ```` ```! ```` blocks) and complete the materialization provenance (`skill_hash`, per-component `removed_bytes`). Add an end-to-end Pi smoke execution test (stubbed `pi` binary) proving the materialized ablated skill is what actually runs, plus coverage for the script/asset/reference-both/multi-root paths and symlink-escape containment.
- Add `docs/skill-ablation-spec.md` (materialized-ablation design) and index it from the README.
- Add `docs/vocabulary.md` glossary and `docs/evals-are-not-tests.md` conceptual page; link both from the README documentation map. Docs only; no behavior changes.
- Add `docs/academic-grounding.md` mapping harness vocabulary to its research constructs (treatment effect, behavioral testing, construct validity, shortcut learning, contamination), qualify `adversarial` as a contrast set, and define case-polarity and negative-delta glossary entries. Docs only; no behavior changes.

## v0.4.2 — 2026-06-12

- Tighten README and trace-aware spec language after the v0.4.1 runner release; no behavior changes.
- Add trace-runner lessons on no-skill workspace isolation, trace-backed process assertions, runner-shape normalization, and post-release spec tense.
- Add a `good-repo` effectiveness audit, package keywords/classifiers, and project URLs.
- Add `skill-benchmark token-overhead` for static skill footprint, paired runtime token deltas, and objective lift per 1k extra tokens.

## v0.4.1 — 2026-06-11

- Add variant-scoped assertions with `variants` / `only_variants` / `except_variants`.
- Ground trace import in live Pi and Codex event shapes: Pi `message_end` usage aliases and Codex `item.completed` / `command_execution` JSONL.
- Add a shared trace artifact writer and use it for `import-trace`, `run-codex`, Jetty result import, Pi smoke runs, and optional Pi trigger traces.
- Harden the Adewale Pi smoke runner by executing in an isolated temporary workspace with only allowed files for each variant.
- Add `skill-pi-trigger-eval --trace-runs` for per-query trace artifacts.

## v0.4.0 — 2026-06-11

- Add GitHub Actions CI for Python 3.10, 3.11, and 3.12.
- Add contributor guidance, PR template, and issue forms.
- Tighten README quick start with expected output landmarks and repo-surface links.
- Add a trace-aware eval proposal covering runner trace artifacts, process/efficiency assertions, Codex adapter support, richer reporting, dataset audits, structured judges, and skill-profile checks.
- Implement the first trace-aware harness slice: `import-trace`, `run-codex`, process/efficiency assertion types, telemetry-aware benchmark summaries, paired deltas/normalized gain, taxonomy slice summaries, taxonomy audit warnings, and `profile-skill`.

## v0.3.0 — 2026-06-10

- Add opt-in `script` assertions gated by `--allow-scripts`.
- Add prompt/assertion leakage lint with non-fatal default warnings and `--strict-leakage` for failure mode.
- Add `skill-benchmark judge` with `--judge-cmd`, repeated judge runs, transcript writing, and JSON result parsing.
- Add ablation variant support in the Adewale Pi smoke runner.

## v0.2.0 — 2026-06-10

- Add Jetty adapter phase one: `export-jetty`, `run-jetty`, `import-jetty-results`, and dry-run support.
- Ground Jetty request shape in Jetty docs and `jettyio/jettyio-skills` conventions.
- Add mocked upload/submit/poll coverage and hidden-prompt placeholder safety.

## v0.1.1 — 2026-06-09

- Switch public installation instructions to `uv` / `uvx`.

## v0.1.0 — 2026-06-09

- Initial standalone shared skill eval harness.
- Add manifest validation, answer-key-safe task preparation, deterministic grading, benchmarking, and reports.
