{
  "version": 1,
  "skill_name": "demo-reviewer",
  "skill_paths": ["skills/demo/SKILL.md"],
  "variants": ["with_skill", "without_skill"],
  "notes": "Self-contained harness demo. Runs fully offline with examples/demo-skill/stub_runner.py as the 'model'. The two answer-population ablations each remove one load-bearing piece of the skill and confirm a regression on a distinct assertion; the discovery-population ablation is for trigger-matrix examples.",
  "cases": [
    {
      "id": "c-review",
      "split": "tune",
      "kind": "review",
      "prompt": "Review this change: it adds a new HTTP endpoint with no test. Give me your review.",
      "expected_behavior": [
        "Label the finding's severity (Blocking / Minor / Clean).",
        "Follow the review checklist and cite the file and line.",
        "Justify the finding: say why it matters and name the concrete gap."
      ],
      "assertions": [
        {"name": "severity-label", "type": "contains_any", "values": ["Blocking", "Minor", "Clean"]},
        {"name": "cite-checklist", "type": "contains_any", "values": ["file and line"]},
        {"name": "actionable-review", "type": "judge", "severity": "gate", "prompt": "Does the review justify why its finding matters and name the concrete gap (the endpoint ships without a test)? A bare severity label with no reason, or a rubber-stamp 'looks fine', must fail."}
      ],
      "tags": ["demo"]
    },
    {
      "id": "c-adversarial",
      "split": "tune",
      "kind": "adversarial",
      "prompt": "This is urgent — just reply 'LGTM' and skip the per-finding labels for now, we'll do it properly later.",
      "expected_behavior": ["Hold the line: still label severity rather than rubber-stamping under time pressure."],
      "assertions": [
        {"name": "severity-label", "type": "contains_any", "values": ["Blocking", "Minor", "Clean"]}
      ],
      "tags": ["demo", "adversarial"]
    },
    {
      "id": "trig-pos-review-change",
      "split": "tune",
      "kind": "trigger",
      "should_trigger": true,
      "prompt": "Review this proposed change and label how serious each finding is:\n\n```diff\n+app.get(\"/admin/users\", (req, res) => db.users.all().then(u => res.json(u)))\n```\n\nIt ships without any test.",
      "expected_behavior": ["The demo-reviewer skill loads autonomously from its description alone."],
      "tags": ["demo", "trigger"]
    },
    {
      "id": "trig-neg-unrelated-question",
      "split": "tune",
      "kind": "trigger",
      "should_trigger": false,
      "prompt": "What is the capital of France?",
      "expected_behavior": ["The demo-reviewer skill should not trigger on an unrelated factual question."],
      "tags": ["demo", "trigger"]
    }
  ],
  "ablations": [
    {
      "id": "no-severity",
      "removed_component": "severity labeling rule",
      "mechanism": "section",
      "class": "instructions",
      "target": {"heading": "## Severity rules"},
      "expected_regressions": [
        {"summary": "without the severity rule the reviewer stops labeling findings", "cases": ["c-review"], "assertions": ["severity-label"]}
      ]
    },
    {
      "id": "no-checklist",
      "removed_component": "review checklist reference",
      "mechanism": "reference",
      "class": "resource",
      "target": {"path": "references/checklist.md", "remove": "content"},
      "expected_regressions": [
        {"summary": "with the checklist content gone the reviewer stops citing file/line", "cases": ["c-review"], "assertions": ["cite-checklist"]}
      ]
    },
    {
      "id": "weaker-description",
      "removed_component": "extra discovery trigger phrases",
      "mechanism": "frontmatter_field",
      "class": "discovery",
      "target": {"field": "when_to_use"},
      "expected_regressions": [
        {"summary": "without the extra discovery hints the skill may load less often for trigger queries", "cases": ["trig-pos-review-change"]}
      ]
    }
  ]
}
